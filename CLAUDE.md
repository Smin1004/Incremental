# 프로젝트 규칙

이 저장소에서 작업하는 모든 세션(기획, 구현)에 적용된다.

## 커밋 메시지

- **커밋 메시지는 한국어로 쓴다.** 제목과 본문 모두.
- 코드 식별자, 파일명, 문서 절 번호(예: `12 §8`)는 원문 그대로 둔다.
- `Co-Authored-By:` 같은 트레일러 줄은 형식을 바꾸지 않는다.
- 이미 올라간 영어 커밋은 고치지 않는다. 이후 커밋부터 적용한다.

예시

```
스킬트리 1단계: 노드 데이터 모델, 관문 기반 티어 해금, 세이브 v2

- UpgradeDef를 NodeDef로 교체하고 같은 statId 노드를 합산
- unlockedMaxTier는 로드 시 관문 노드 레벨에서 재계산
- 12 §10 규칙 변경 6건 반영
```
